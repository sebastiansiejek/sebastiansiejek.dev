import styled from 'styled-components'

const ContentStyled = styled.div`
  > {
    *,
    pre {
      line-height: 1.8rem;
      &:not(:first-child) {
        margin-top: 2.4rem;
      }
    }
  }
`

export default ContentStyled
